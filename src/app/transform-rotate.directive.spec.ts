import { TransformRotateDirective } from './transform-rotate.directive';

describe('TransformRotateDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformRotateDirective();
    expect(directive).toBeTruthy();
  });
});
